#include <iostream>
using namespace std;
int main() {
 int x, y;
 cout << "x";
 cin >> x;
 cout << "y";
 cin >> y;
 
 if (x > 0 && y > 0) {
 cout << "I ";
 }
 else if (x < 0 && y>0) {
 cout << "II";
      }
       else if (x < 0 && y < 0) {
    cout << "III";
 }
 else
 cout << "IV";
}
